---
applies_to: safety_goal
required_before: [reviewed]
---

# Safety goal review checklist

- [ ] `:asil:` is set to A, B, C, or D (QM not allowed for safety goals)
- [ ] `:mitigates:` names at least one real `hazard` ID
- [ ] `:safe_state:` is defined and reachable
- [ ] Goal is decomposed by at least one `fsr`
