---
applies_to: impl
required_before: [reviewed]
---

# Implementation review checklist

- [ ] `:implements:` names a real `spec`, `component`, or `interface` ID
- [ ] Title names the implementing module / function (or its commit anchor)
- [ ] Body summarizes the realised behavior, not unrelated context
- [ ] At least one `test` verifies the same `spec`
