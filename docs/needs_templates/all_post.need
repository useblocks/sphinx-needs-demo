
.. dropdown:: Object traceability details: {{title}}

    .. needflow::
        :filter:  "{{id}}"==id or "{{id}}" in links or "{{id}}" in links_back or "{{id}}" in author_back

    .. needtable::
        :style: table
        :filter:  "{{id}}"==id or "{{id}}" in links or "{{id}}" in links_back or "{{id}}" in author_back
        :columns: id, title, type, author
